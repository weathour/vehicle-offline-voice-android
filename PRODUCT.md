# Product

## Register

product

## Users

车辆语音模块开发者、上车测试人员、现场联调人员。使用场景是在电脑模拟环境、手机真机、车辆局域网、车载屏幕之间切换，快速确认只读车辆状态问答是否能读到 Redis/protobuf 数据并通过离线语音回复。

## Product Purpose

Vehicle Offline Voice 是一个 Android 离线车载语音测试与集成 APK。它让测试人员填写车辆或电脑 Redis 的 IP 和端口，执行连接诊断，启动真实麦克风语音服务，并验证车速、电量、胎压、告警、协作场景等只读车辆状态问答。成功标准是上车后少操作、可诊断、可复现，验证通过后可迁移安装到车载屏幕。

## Brand Personality

可靠、清晰、工程化。界面应像测试仪表而不是演示页面：状态明确、按钮含义直接、错误可定位。

## Anti-references

不要做成炫技型大屏、营销页或复杂控制台。避免把开发 mock、日志、脚本入口堆在第一屏；避免现场人员在多个按钮之间猜测启动顺序；避免只显示“失败”而不指出是网络、Redis、key 缺失还是 protobuf 解码问题。

## Design Principles

1. 上车优先：第一屏服务于“填 IP/端口、测试连接、启动语音”。
2. 诊断可读：连接、key、解码、语音识别、TTS 回复要能一眼分层定位。
3. 安全只读：当前阶段只读车辆状态，不暴露真实车控动作入口。
4. 模拟不打扰：保留模拟与开发入口，但折叠到开发调试区。
5. 现场可复现：测试记录和日志应能复制、截图或通过 logcat 复盘。

## Accessibility & Inclusion

面向车内和现场调试环境，使用高对比文本、明确按钮标签、足够大的触控区域。减少依赖颜色的状态表达，所有关键状态都用文字说明。避免过度动画，确保亮屏、弱网、嘈杂语音环境下仍能读懂界面。
